import os
from dotenv import load_dotenv
from langchain_openai import ChatOpenAI
from langgraph.prebuilt import create_react_agent
from tools import search_internet, scrape_website

# Загружаем ключи из .env
load_dotenv()

# Инициализируем LLM
llm = ChatOpenAI(model="gpt-4o-mini", temperature=0.2)

# Собираем инструменты исследователя
tools = [search_internet, scrape_website]

# --- СИСТЕМНЫЙ ПРОМПТ (с правильными закрывающими кавычками) ---
SYSTEM_PROMPT = """Ты — автономный ИИ-исследователь продвинутого уровня.
Твоя задача — провести глубокое исследование по заданной теме и написать структурированный итоговый отчёт.

ТВОЙ АЛГОРИТМ РАБОТЫ:
1. Поиск (Search): Сделай минимум 3 разных поисковых запроса по теме.
2. Чтение (Scrape): Прочитай минимум 3-4 самые релевантные статьи по найденным ссылкам.
3. Анализ (Analyze): Сопоставь информацию. Исключи дубликаты. Обязательно найди ПРОТИВОРЕЧИЯ или скрытые факты.
4. Синтез (Synthesize): Напиши финальный отчёт на русском языке в формате Markdown.

СТРУКТУРА ОТЧЁТА:
# [Тема исследования]
## Введение
## Основные разделы (с конкретными цифрами и фактами)
## Скрытые проблемы и противоречия
## Выводы
## Источники (список точных URL-адресов, которые ты прочитал)

Опирайся ТОЛЬКО на факты, полученные из интернета. Не выдумывай информацию.
"""

# Создаем агента
agent_executor = create_react_agent(llm, tools)